# 34 - Escreve um programa que pergunte o salário de um funcionário e calcule o seu aumento
# Para salários superiores à R$1250,00, calcule um aumento de 10%
# Para os inferiores ou iguais, o aumento é de 15%

salario = float(input('Qual o seu salário? '))

if salario >= 1250:
    aumento = salario * 0.1
    print(f'Você receberá um aumento de R${aumento}')
    
else:
    aumento = salario * 0.15
    print(f'Você receberá um aumento de R${aumento}')
