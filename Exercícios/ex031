# 31 - Desenvolva um programa que pergunte a distância de uma viagem em km
# Calcule o preço da passagem, cobrando R$0,50 por km para viagens até 200km
# e R$0,45 para viagens mais longas

dist = int(input('Qual a distância em km? '))

if dist <= 200:
    preco = dist * 0.5
    print(f'A passagem custará R${preco}')
else:
    preco = dist * 0.45
    print(f'A passagem custará R${preco}')
    
# OUTRA FORMA

dist = int(input('Qual a distância em km? '))

preco = dist * 0.5 if dist <= 200 else dist * 0.45

print(f'A passagem custará R${preco}')
